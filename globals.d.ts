// Side-effect import of `*.css` (e.g. `import './globals.css'` in layout.tsx)
// is a Next.js feature; declare it so the TS language server stops flagging
// the import as a missing module.
declare module '*.css';
